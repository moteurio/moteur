# API `src/plugins`

This directory is intentionally empty. Host plugins are loaded via `MOTEUR_HOST_PLUGINS` (optionally filtered by `MOTEUR_ENABLED_PLUGINS`) as documented in [Configuration](../../../../docs/Configuration.md), then mounted by `packages/api/src/index.ts`.
