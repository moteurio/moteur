/**
 * Persisted JWT / `User.roles` value for platform operators (full project list,
 * blueprints, seed, usage, asset migration). Kept as a named constant so API
 * code does not repeat the literal role slug.
 */
export const OPERATOR_ROLE_SLUG = 'admin';
